{
  "name": "jest-ratchet",
  "version": "1.0.2",
  "description": "Ratchet up code coverage - keep test coverage going only one direction -- up",
  "keywords": [
    "jest",
    "coverage",
    "test",
    "test-coverage",
    "jest-ratchet"
  ],
  "author": "Markis Taylor <m@rkis.cc>",
  "main": "index.js",
  "engines": {
    "node": ">=4"
  },
  "license": "MIT",
  "jest": {
    "collectCoverage": true,
    "coverageReporters": [
      "json-summary",
      "lcovonly"
    ],
    "coverageThreshold": {
      "global": {
        "branches": 75,
        "functions": 75,
        "lines": 75,
        "statements": 75
      }
    },
    "testPathIgnorePatterns": [
      "/node_modules/",
      "/test-setup/"
    ],
    "transform": {
      "^.+\\.tsx?$": "ts-jest"
    },
    "testRegex": "^.+\\.test\\.tsx?$",
    "moduleFileExtensions": [
      "js",
      "ts",
      "tsx"
    ],
    "verbose": true
  },
  "scripts": {
    "build": "rollup -c",
    "prepublish": "tslint src/*.ts && jest && rollup -c",
    "test": "tslint src/*.ts && jest --no-cache"
  },
  "devDependencies": {
    "@types/jest": "22.0.1",
    "@types/node": "9.3.0",
    "jest": "22.0.6",
    "rollup": "0.54.0",
    "rollup-plugin-typescript": "0.8.1",
    "ts-jest": "22.0.1",
    "tslint": "5.9.1",
    "typescript": "2.6.2"
  },
  "dependencies": {
    "yargs-parser": "8.1.0"
  }
}
